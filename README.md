# Vela — Date Night, Figured Out

154+ curated date ideas, personalized to your partner. Take the quiz, get the plan, send the invite.

**Live:** https://vallotaventures.com/vela  
**Stack:** React (CRA), single-file architecture, deployed on Vercel  
**Affiliate:** Amazon Associates (`vallotaventur-20`)

## Dev

```bash
npm install
npm start        # localhost:3000
npm run build    # production build (use CI=false)
```

## Deploy

Vercel auto-deploys from `main`. Push to deploy.

```bash
git add -A && git commit -m "your message" && git push
```
